namespace Application.Identity.Commands.ForgotPassword;

/// <summary>No fields on purpose — the response is identical whether or not the email was registered.</summary>
public sealed record ForgotPasswordResult;
