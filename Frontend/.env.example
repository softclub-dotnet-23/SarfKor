# Base URL of the Sarfkor backend (Backend/src/WebApi).
# Defaults to http://localhost:5135 in code if this is unset — copy this file
# to .env.local and adjust only if your backend runs on a different port/host.
VITE_API_BASE_URL=http://localhost:5135
